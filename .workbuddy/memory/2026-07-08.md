# 2026-07-08 工作记录

## 任务：分析 122火箭弹 EEPROM(24C64) 并推进固件驱动（用户选方向 A）
- 读取 `D:/work_project/资料/122火箭弹EEPROM_24C64.PDF`（芯伯乐 XBLW 24C64 规格书，18页扫描件，已渲染到 `122_eeprom_pages/`）。
- 关键参数：8KB(8192B)，256页×32B/页，13位地址(16位I2C传输)，7位设备地址 0x50(A2/A1/A0接地)，Vcc 1.8-5.5V，SCL≤1MHz(2.5V以上)，写周期5ms，100万次擦写，上电延迟t_POR≤500μs。
- 探明工程现状：底层驱动 `My_Top/System/BL24C16F.cpp/.hpp` **已存在且配置正确对应 24C64**（PAGE=32 / SIZE=8192 / ADDR_SIZE=16BIT），仅类名仍叫 BL24C16F（2KB型号）造成混淆。
- I2C 硬件：CubeMX 中 I2C1，SCL=PA15, SDA=PB7, Fast模式(400kHz, Timing=0x20B21E5A)。bsp.hpp:38 `#define EEPROM_hi2c &hi2c1`。Task_Test.cpp:33 已实例化 `BL24C16F EEPROM1(EEPROM_hi2c);` 但仅声明、测试循环里未实际读写。
- 结论：方向 A 真正缺的是**第二层结构化内存映射/数据读写层**（PID参数/零位/校准落盘），底层驱动已就绪。
- 下一步：与用户确认要持久化的数据字段后，新建 `EepromStorage` 数据层（封装 BL24C16F + 内存映射表 + 版本头），供4个 DC_Motor 加载/保存参数。
